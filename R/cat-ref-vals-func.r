# Calculate categorical covariate reference values
# ------------------------------------------------------------------------------
cat_ref_vals.func <- function(vars){
  full.cat[[vars]][1]
} # cat.ref.assign
